apiVersion: v1
kind: Pod
metadata:
  name: sample-ingress
  namespace: my-namespace
spec:
  rules:
  - host: foo.bar.com
    http:
      paths:       
      - pathType: Prefix
        path: /foo
        backend:
          service:
            name: hello-world-sample
            port:
              number: 4200